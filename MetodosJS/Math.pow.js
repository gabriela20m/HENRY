// Elevar 2 a la potencia de 3
let base = 2;
let exponente = 3;
let resultado = Math.pow(base, exponente);
console.log(`${base} elevado a la potencia de ${exponente} es ${resultado}`); // 2 elevado a la potencia de 3 es 8
